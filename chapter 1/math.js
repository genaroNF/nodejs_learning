// here we are adding each function to the export of this file,
// it works the same way as the method commented below
exports.add = (a, b) => a + b
exports.subtract = (a, b) => a - b
exports.multiply = (a, b) => a * b
exports.divide = (a, b) => a / b

// here we export the functions we created above
// module.exports = {
//     add,
//     subtract,
//     multiply,
//     divide,
// }